# Dev env for compiling SR with homcc

This image can be used to build SR with our distributed compiler [homcc](https://github.com/celonis/homcc).

## Usage

```bash
./run.sh    # This will build the image, run it and build SR
```