-- name: test_celonis_array_functions_with_null_literal

SELECT CELONIS_ARRAY_FIRST(NULL);

SELECT CELONIS_ARRAY_FIRST(IF(rand(), NULL, NULL));

SELECT CELONIS_ARRAY_LAST(NULL);

SELECT CELONIS_ARRAY_LAST(IF(rand(), NULL, NULL));

SELECT CELONIS_ARRAY_AVG(NULL);

SELECT CELONIS_ARRAY_AVG(IF(rand(), NULL, NULL));

SELECT CELONIS_ARRAY_COUNT(NULL);

SELECT CELONIS_ARRAY_COUNT(IF(rand(), NULL, NULL));

SELECT CELONIS_ARRAY_COUNT_DISTINCT(NULL);

SELECT CELONIS_ARRAY_COUNT_DISTINCT(IF(rand(), NULL, NULL));

SELECT CELONIS_ARRAY_BOOL_OR(NULL);

SELECT CELONIS_ARRAY_BOOL_OR(IF(rand(), NULL, NULL));

SELECT CELONIS_APPLY_KMEANS_MODEL(NULL, "1.0");

SELECT CELONIS_APPLY_KMEANS_MODEL(IF(rand(), NULL, NULL), "1.0");
